<%= form_for(customer_quote) do |f| %>
  <% if customer_quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_quote.errors.count, "error") %> prohibited this customer_quote from being saved:</h2>

      <ul>
      <% customer_quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>

  <div class="field">
    <%= f.label :customer_po_id %>
    <%= f.text_field :customer_po_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
